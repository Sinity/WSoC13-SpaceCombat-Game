CMAKE:
	Not working in windows yet(problems with linking with sfml).  For Linux:
		mkdir build
		cd build
		cmake ..
		make
		-copy resources folder to folder with compiled game. 
		-create empty logz folder in folder with compiled game


QMAKE (QT Creator):
	WSoC.pro is for Linux, WSoC_windows.pro is for Windows

	-set pathes to SFML libraries in WSoC.pro or WSoC_windows.pro(depending on which system you have)
	-Copy folder resources to folder with compiled game. Also, create empty 'logz' folder here.
	-For windows, also copy dlls from binaries folder there.
	 